import {Container, Typography} from "@mui/material";

const LoadingPage = () => {

    return(
        <Container sx={{
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            minHeight: '90vh'
        }}>

        </Container>
    )

}

export {LoadingPage}